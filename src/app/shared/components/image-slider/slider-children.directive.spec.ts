import { SliderChildrenDirective } from './slider-children.directive';

describe('SliderChildrenDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderChildrenDirective();
    expect(directive).toBeTruthy();
  });
});

import { OptionValueDirective } from './option-value.directive';

describe('OptionValueDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionValueDirective();
    expect(directive).toBeTruthy();
  });
});

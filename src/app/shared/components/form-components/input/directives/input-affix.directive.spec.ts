import { InputAffixDirective } from './input-affix.directive';

describe('InputAffixDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAffixDirective();
    expect(directive).toBeTruthy();
  });
});

import { PopoverOptionDirective } from './popover-option.directive';

describe('PopoverOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverOptionDirective();
    expect(directive).toBeTruthy();
  });
});

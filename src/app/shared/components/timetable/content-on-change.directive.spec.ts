import { ContentOnChangeDirective } from './content-on-change.directive';

describe('ContentOnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});

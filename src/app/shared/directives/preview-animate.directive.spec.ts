import { PreviewAnimateDirective } from './preview-animate.directive';

describe('PreviewAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewAnimateDirective();
    expect(directive).toBeTruthy();
  });
});

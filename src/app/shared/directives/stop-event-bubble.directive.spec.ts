import { StopEventBubbleDirective } from './stop-event-bubble.directive';

describe('StopEventBubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new StopEventBubbleDirective();
    expect(directive).toBeTruthy();
  });
});

import { SetOVerflowDirective } from './set-overflow.directive';

describe('SetOVerflowDirective', () => {
  it('should create an instance', () => {
    const directive = new SetOVerflowDirective();
    expect(directive).toBeTruthy();
  });
});

import { createAction } from '@ngrx/store';

export const incrementProgress = createAction(
  '[] incrementProgress'
)
export const selectUserModalState = createAction(
  '[Dashboard] selectUserModalState'
)
